import React from "react";
import { View, Text, SafeAreaView } from "react-native";


export default App=()=>{
    //console.log("Native Props",props.data);
    return(
        <SafeAreaView>
            <Text> hellosakdjasl</Text>
        </SafeAreaView>
    );
}